{
  "private": true,
  "workspaces": ["my-next-app"],
  "scripts": {
    "frontend:dev": "cd my-next-app && npm run dev",
    "frontend:build": "cd my-next-app && npm run build",
    "frontend:start": "cd my-next-app && npm run start",
    "build": "npm run frontend:build",
    "start": "npm run frontend:start"
  }
}
